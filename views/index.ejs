<!doctype html>
<html lang="sr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Smart Garage Dashboard</title>

  <link rel="stylesheet" href="/style.css" />

  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>

  <!-- Google Charts -->
  <script src="https://www.gstatic.com/charts/loader.js"></script>
</head>
<body>
  <header class="topbar">
    <div>
      <h1>Smart Garage</h1>
      <div class="muted">
        Uređaj: <b id="deviceId"><%= deviceId %></b>
        <% if (name || place) { %>
          <span> • <%= name %> <%= place ? "(" + place + ")" : "" %></span>
        <% } %>
      </div>
      <% if (description) { %>
        <div class="muted small"><%= description %></div>
      <% } %>
    </div>
    <div style="display:flex; gap:12px; align-items:center;">
      <a class="btn" href="/devices">Moji uređaji</a>
      <a class="btn" href="/logout">Logout</a>
      <div class="badge" id="statusBadge">Nema podataka</div>
    </div>
  </header>

  <main class="grid">
    <section class="card">
      <h2>Trenutno stanje</h2>
      <div class="kv">
        <div class="k">Door</div><div class="v" id="door">—</div>
        <div class="k">LED</div><div class="v" id="led">—</div>
        <div class="k">Distance (cm)</div><div class="v" id="dist">—</div>
        <div class="k">PIR</div><div class="v" id="pir">—</div>
        <div class="k">LDR</div><div class="v" id="ldr">—</div>
        <div class="k">Night</div><div class="v" id="night">—</div>
        <div class="k">t_left (ms)</div><div class="v" id="tleft">—</div>
        <div class="k">Last update</div><div class="v" id="last">—</div>
      </div>
    </section>

    <section class="card">
      <h2>Kontrole</h2>
      <div class="kv" style="grid-template-columns: 1fr;">
        <button class="btn" id="btnOpen">Otvori vrata</button>
        <button class="btn" id="btnClose">Zatvori vrata</button>
        <div style="height:10px"></div>
        <button class="btn" id="btnLedOn">LED ON</button>
        <button class="btn" id="btnLedOff">LED OFF</button>
      </div>
      <div class="muted small">Komande idu na uređaj (Pico) preko servera.</div>
    </section>

    <section class="card">
      <h2>Lokacija</h2>
      <div id="map"></div>
      <div class="muted small">Lokacija je ona koju si unela/uhvatila sa telefona pri registraciji uređaja.</div>
    </section>

    <section class="card">
      <h2>Istorija (chart)</h2>
      <div id="chart" style="width:100%; height:320px;"></div>
      <div class="muted small">Primer: poslednjih 200 poruka (distance_cm).</div>
    </section>
  </main>

  <script>
    window.__CFG__ = {
      deviceId: "<%= deviceId %>",
      lat: <%= lat %>,
      lng: <%= lng %>
    };
  </script>
  <script src="/app.js"></script>
</body>
</html>
